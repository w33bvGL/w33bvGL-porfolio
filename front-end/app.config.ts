export default defineAppConfig({
    ui: {
        primary: 'lime',
        gray: 'neutral',
        notifications: {
            position: 'top-0 bottom-[unset] -translate-x-1/2 left-1/2',
        },
    },
});
